class Solution(object):
    def isPalindrome(self, x):
        a = str(x)
        if int(a) < 0:
            return(False) 
        if a[::-1] == a:
            return( True)
        else:
            return(False)
        
        
